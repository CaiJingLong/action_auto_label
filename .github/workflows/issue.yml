name: 'On issue'
on: 
  issues:
    types: [opened, reopened, edited]
    branches: 
      - 'main'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
