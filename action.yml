name: 'Auto label'
description: 'Automation generate label for issues.'
author: 'Caijinglong'
inputs:
  user_name:
    required: true
    description: 'User name'
  github_token:
    required: true
    description: 'Your github token, it is secrets.GITHUB_TOKEN.'
runs:
  using: 'node12'
  main: 'dist/index.js'
